DropList

A barebones simple hide/show section/list directive
Simply trancludes the content within the directive source, no replacement, and adds a button with a header to toggle the hide/show of the content.

Can be done using styled containers or using a list element